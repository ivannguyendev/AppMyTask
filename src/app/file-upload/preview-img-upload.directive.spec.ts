import { PreviewImgUploadDirective } from './preview-img-upload.directive';

describe('PreviewImgUploadDirective', () => {
  it('should create an instance', () => {
    const directive = new PreviewImgUploadDirective();
    expect(directive).toBeTruthy();
  });
});
